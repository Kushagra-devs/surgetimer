import { PageFrame } from '../../components/page-frame';

export default function RidersPage() {
  return (
    <PageFrame title="Riders" description="Rider profiles and import workflows live here.">
      <div className="metric-card">CSV templates and preview-confirm import flow are planned into the API scaffold.</div>
    </PageFrame>
  );
}

