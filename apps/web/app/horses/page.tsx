import { PageFrame } from '../../components/page-frame';

export default function HorsesPage() {
  return (
    <PageFrame title="Horses" description="Horse profiles, stables, and notes.">
      <div className="metric-card">The domain structure is in Prisma and the UI route is ready for expansion.</div>
    </PageFrame>
  );
}

